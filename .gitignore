# .NET build output
bin/
obj/

# Visual Studio / Rider
.vs/
.idea/
*.user
*.suo
*.userosscache
*.sln.docstates

# .NET user secrets (stored outside repo at %APPDATA%\Microsoft\UserSecrets)
# Already safe, but listed for clarity — never commit secrets here:
# appsettings.Development.json only if it contains real keys

# Environment / secrets files — never commit these
.env
.env.local
.env.*.local
appsettings.Local.json
appsettings.Production.json

# Node
RagDemo.Web/node_modules/
RagDemo.Web/dist/
RagDemo.Web/.vite/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
